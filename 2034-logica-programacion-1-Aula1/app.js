alert('Hola de nuevo, mundo'); //función, lo que va dentro del paréntesis se llaman parámetros, el cual es un valor, en este caso texto.

// prompt("Introduce un número, por favor:")

// const, let y var
//variables escritas en CamelCase
let numeroSecreto = 6;
let numeroUsuario = prompt("Me indicas un numero por favor:");

console.log(numeroUsuario);

if (numeroUsuario == numeroSecreto) {
    alert('Acertaste el numero');
}
